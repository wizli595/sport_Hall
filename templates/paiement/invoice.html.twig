<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice #{{ paiement.id }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: #f8f9fa;
            padding: 40px;
        }
        .invoice-container {
            max-width: 800px;
            margin: auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.1);
        }
        .invoice-header {
            border-bottom: 2px solid #007bff;
            padding-bottom: 10px;
        }
        .invoice-title {
            font-size: 2rem;
            color: #007bff;
            font-weight: bold;
        }
        .invoice-details {
            margin-top: 20px;
        }
        .invoice-footer {
            margin-top: 30px;
            text-align: center;
            font-size: 0.9rem;
            color: #6c757d;
        }
        .invoice-table th {
            background-color: #007bff;
            color: white;
        }
    </style>
</head>
<body>
    <div class="invoice-container">
        <!-- Invoice Header -->
        <div class="invoice-header text-center">
            <h1 class="invoice-title">Invoice</h1>
            <p class="text-muted">Transaction ID: #{{ paiement.id }}</p>
        </div>

        <!-- Invoice Information -->
        <div class="invoice-details">
            <div class="row">
                <div class="col-md-6">
                    <h5>Issued By:</h5>
                    <p><strong>Sports Hall Management</strong><br>
                        123 Gym Street, Fitness City<br>
                        Email: support@sportshall.com<br>
                        Phone: +123 456 789
                    </p>
                </div>
                <div class="col-md-6 text-end">
                    <h5>Billed To:</h5>
                    <p>
                        <strong>{{ paiement.abonne.name }}</strong><br>
                        Email: {{ paiement.abonne.user.email }}<br>
                        Phone: {{ paiement.abonne.tele }}
                    </p>
                </div>
            </div>
        </div>

        <!-- Payment Summary Table -->
        <table class="table invoice-table mt-4">
            <thead>
                <tr>
                    <th>Description</th>
                    <th class="text-center">Amount (€)</th>
                    <th class="text-center">Payment Date</th>
                    <th class="text-center">Mode</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Subscription Payment</td>
                    <td class="text-center">{{ paiement.montant }}</td>
                    <td class="text-center">{{ paiement.payDate|date('Y-m-d H:i:s') }}</td>
                    <td class="text-center">{{ paiement.modePay }}</td>
                </tr>
            </tbody>
        </table>

        <!-- Invoice Footer -->
        <div class="invoice-footer">
            <p>Thank you for your payment! Stay active and healthy!</p>
            <p><strong>Sports Hall Management</strong></p>
        </div>
    </div>
</body>
</html>

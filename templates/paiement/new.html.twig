{% extends 'base.html.twig' %}

{% block title %}New Paiement
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<div class="card shadow-lg p-4">
			<div class="card-header bg-success text-white text-center">
				<h2>
					<i class="bi bi-plus-circle"></i>
					Create New Paiement
				</h2>
			</div>
			<div class="card-body">
				<div class="alert alert-info text-center" role="alert">
					<i class="bi bi-info-circle-fill"></i>
					Fill out the form to add a new payment record.
				</div>

				{# Form for Creating New Paiement #}
				{{ form_start(form, { 'attr': { 'class': 'needs-validation', 'novalidate': 'novalidate' } }) }}

				<div class="mb-4">
					{{ form_label(form.montant, 'Montant (€)', {'label_attr': {'class': 'form-label'}}) }}
					{{ form_widget(form.montant, {'attr': {'class': 'form-control', 'placeholder': 'Enter amount'}}) }}
					<div class="form-text">Specify the payment amount in euros.</div>
					{{ form_errors(form.montant) }}
				</div>

				<div class="mb-4">
					{{ form_label(form.payDate, 'Payment Date', {'label_attr': {'class': 'form-label'}}) }}
					{{ form_widget(form.payDate, {'attr': {'class': 'form-control', 'placeholder': 'Select a date'}}) }}
					<div class="form-text">Choose the date of the payment.</div>
					{{ form_errors(form.payDate) }}
				</div>

				<div class="mb-4">
					{{ form_label(form.modePay, 'Payment Mode', {'label_attr': {'class': 'form-label'}}) }}
					{{ form_widget(form.modePay, {'attr': {'class': 'form-control', 'placeholder': 'e.g., Card, Cash'}}) }}
					<div class="form-text">Enter the mode of payment (e.g., Cash, Card).</div>
					{{ form_errors(form.modePay) }}
				</div>

				<div class="mb-4">
					{{ form_label(form.abonne, 'Abonné', {'label_attr': {'class': 'form-label'}}) }}
					{{ form_widget(form.abonne, {'attr': {'class': 'form-select'}}) }}
					<div class="form-text">Select the subscriber for this payment.</div>
					{{ form_errors(form.abonne) }}
				</div>

				<div class="text-center mt-4">
					<button type="submit" class="btn btn-success btn-lg shadow-sm">
						<i class="bi bi-check-circle"></i>
						Save
					</button>
					<a href="{{ path('app_paiement_index') }}" class="btn btn-outline-secondary btn-lg shadow-sm">
						<i class="bi bi-arrow-left-circle"></i>
						Back to List
					</a>
				</div>

				{{ form_end(form) }}
			</div>
		</div>
	</div>

	{# Additional Styles and Animations #}
	<style>
		/* Card Design */
		.card {
			border-radius: 15px;
			background-color: #f8f9fa;
			animation: fadeIn 0.5s ease-in-out;
		}

		.card-header {
			border-radius: 15px 15px 0 0;
			padding: 1.5rem;
			font-size: 1.5rem;
			font-weight: bold;
		}

		/* Form Styling */
		.form-label {
			font-weight: bold;
		}

		.form-control,
		.form-select {
			border-radius: 10px;
			transition: all 0.3s ease-in-out;
		}

		.form-control:focus,
		.form-select:focus {
			border-color: #007bff;
			box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
		}

		/* Buttons */
		.btn {
			display: inline-flex;
			align-items: center;
			gap: 0.5rem;
		}

		.btn:hover {
			transform: scale(1.05);
			transition: transform 0.2s;
		}

		.btn-outline-secondary:hover {
			background-color: #6c757d;
			color: white;
		}

		.btn-success:hover {
			background-color: #28a745;
			color: white;
		}

		/* Alert Styling */
		.alert {
			font-size: 1rem;
			border-radius: 10px;
		}

		/* Fade-In Animation */
		@keyframes fadeIn {
			from {
				opacity: 0;
				transform: translateY(-10px);
			}
			to {
				opacity: 1;
				transform: translateY(0);
			}
		}

		/* Responsive Design */
		@media(max-width: 768px) {
			.text-center .btn {
				width: 100%;
				margin-bottom: 1rem;
			}
		}
	</style>
{% endblock %}

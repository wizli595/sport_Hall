<form method="post" action="{{ path('app_abonne_delete', {'id': abonne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ abonne.id) }}">
	<button class="btn btn-danger btn-sm d-flex align-items-center">
		<i class="bi bi-trash-fill me-2"></i>
		Delete
	</button>
</form>

{# Additional Styles #}
<style>
	.btn-danger {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		transition: all 0.2s ease;
	}

	.btn-danger:hover {
		background-color: #d9534f;
		transform: scale(1.05);
	}

	.btn-danger i {
		font-size: 1rem;
	}
</style>
